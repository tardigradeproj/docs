# Heir Docs

Documentation for [Heir](https://github.com/tardigradeproj/heir), a Kubernetes distribution that runs control planes as stateless pods inside a shared management cluster and distributes
Worker nodes components as a single binary.

## Local Development

```bash
yarn
yarn start
```

## Build

```bash
yarn build
```

Static output is written to the `build` directory.
